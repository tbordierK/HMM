function y = efficient_sum(a)
%efficiently computes the log of the sum of the ai
l = log(a);
indice = indice_max(a);

e=0;
for j=1:4
    if j~=indice
        e = e + exp(l(j)-l(indice));
    end
end



end